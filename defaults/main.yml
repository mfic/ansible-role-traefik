---
# defaults file for ansible-role-traefik
traefik:
  docker_network: "webproxy"
  working_directory: "/path"
  api:
    host: "domain"
    users:
      - "user1"
    main: "domain1"
    sans:
      - "domain2"
cloudflare:
  email: "you@mail.com"
  token: "your_token"